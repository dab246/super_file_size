name: super_file_size_example
description: Example usage of the super_file_size package.
publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  super_file_size:
    path: ../
